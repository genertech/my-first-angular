import { KazaroDirective } from './kazaro.directive';

describe('KazaroDirective', () => {
  it('should create an instance', () => {
    const directive = new KazaroDirective();
    expect(directive).toBeTruthy();
  });
});
